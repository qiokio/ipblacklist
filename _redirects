# 确保用户访问管理界面时已经登录
# /admin/* /login.html 302  # 已禁用此规则，改为使用JavaScript验证用户登录状态

# 处理旧路径到新路径的重定向（如果将来需要更改URL结构）
# /old-path /new-path 301

# 确保API文档可通过简短URL访问
/docs /api-docs.html 302

# 保持对旧文档的兼容性
/API-HANDBOOK.md /api-docs.html 302

# API密钥测试工具的简短URL
/keytest /key-test.html 302
/api-test /key-test.html 302

# 设置首页别名
/home / 302

# 任何未匹配的路径会显示自定义404页面
/* /404.html 404 